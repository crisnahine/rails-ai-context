/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/spec/examples.txt
/tmp/
*.gem
/Gemfile.lock
gemfiles/*.lock
# The static-tier fixture app ships a lockfile on purpose: GemIntrospector
# reads it, and a bare `Gemfile.lock` pattern matches at any depth, so this
# one was silently untracked and its spec only passed where it had been
# generated by hand.
!spec/fixtures/static_app/Gemfile.lock
spec/internal/log/

# AI assistant configuration (local only)
.claude/
.superpowers/
CLAUDE.md
docs/superpowers/

# MCP Registry auth tokens
.mcpregistry_*

# Integration test apps
test_apps/
